<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Foreground service permission (required Android 9+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Notification permission (required Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Keep screen on during focus sessions -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Haptics for aborted flights -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- App Blocking usage stats permission (requires user grant via Settings) -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".AeroFocusApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AeroFocus">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AeroFocus">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Flight Timer Foreground Service -->
        <service
            android:name=".service.FlightTimerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- App Blocking Service (runs alongside timer) -->
        <service
            android:name=".service.FocusEnforcerService"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>
